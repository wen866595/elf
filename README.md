elf
===

elf is nio net development framework .
based on elf, i added an RFC6455[WebSocket] server-side implement.

elf 是基于NIO的网络开发框架，遵循Reactor模式，有自己的Buffer层。
在elf 核心的基础上，添加了RFC6455[WebSocket]的一个服务器端实现。